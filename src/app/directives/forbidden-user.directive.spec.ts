import { ForbiddenUserDirective } from './forbidden-user.directive';

describe('ForbiddenUserDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenUserDirective();
    expect(directive).toBeTruthy();
  });
});
